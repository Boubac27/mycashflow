function updateLastSearches() {
  const container = document.querySelector('.last-searches');
  container.innerHTML = "<%= j render 'latest_searches', searches: current_user.searches %>";
}

updateLastSearches();
